<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cetc.netsafe.modules.kg.dao.KgDao">

    <insert id="save" parameterType="cetc.netsafe.modules.kg.model.Movie">
        create(stu:Student:Player{id:2,name:'yyk',class:132})
    </insert>   

    <select id="findById" parameterType="long" resultType="map">
        match(stu:Student) return (stu);
    </select>
    
</mapper>